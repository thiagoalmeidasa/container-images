---
app: bitnami-postgres-pgvecto-rs
semver: true
channels:
  - name: latest
    platforms: ["linux/amd64", "linux/arm64"]
    stable: true
    tests:
      enabled: true
      type: web
      env:
        - "ALLOW_EMPTY_PASSWORD=yes"
