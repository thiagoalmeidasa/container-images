---
app: bitnami-postgres-pgvecto-rs
semver: true
channels:
  - name: latest
    platforms: ["linux/amd64", "linux/arm64"]
    stable: true
    tests:
      enabled: true
      type: cli
      env:
        - "ALLOW_EMPTY_PASSWORD=yes"
        - "NADA=yes"
